[project]
name = "neighborhood-matcher"
version = "0.1.0"
description = "A collection of tools for matching participant-level data to ACS tracts."
readme = "README.md"
requires-python = ">=3.9"

[tool.uv]
workspace = { members = ["version-1/python", "version-3"] }
